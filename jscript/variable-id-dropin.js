// JavaScript Document
               var rrp=0;
               $('.rrp').each(function(){
               rrp++;
               var newID='rrp'+rrp;
               $(this).attr('id',newID);
               //$(this).val($('#rrp').val());
              });
			  
			  var cst=0;
               $('.cst').each(function(){
               cst++;
               var newID='cst'+cst;
               $(this).attr('id',newID);
              // $(this).val($('#cst').val());
              });
			  
			  var rrpd=0;
               $('.rrpd').each(function(){
               rrpd++;
               var newID='rrpd'+rrpd;
               $(this).attr('id',newID);
               //$(this).val($('#rrp').val());
              });
			  
			  var cstd=0;
               $('.cstd').each(function(){
               cstd++;
               var newID='csdt'+cstd;
               $(this).attr('id',newID);
              // $(this).val($('#cst').val());
              });

               var h=0;
               $('.width').each(function(){
               h++;
               var newID='swidth'+h;
               $(this).attr('id',newID);
               //$(this).val($('#widthid').val());
              });
			  
               var i=0;
               $('.length').each(function(){
               i++;
               var newID='slength'+i;
               $(this).attr('id',newID);
               //$(this).val($('#lengthid').val());
              });
			  
			  
			  
			   var j=0;$('.desclist').each(function(){
               j++; var newID='desclist'+j;
               $(this).attr('id',newID);
               });
			  
			   var k=0; $('.desc').each(function(){
			   k++; var newID='desc'+k;
               $(this).attr('id',newID);       
			   });
			   
			   var l=0; $('.invent').each(function(){
			   l++; var newID='invent'+l;
               $(this).attr('id',newID);       
			   });
			   
			   var m=0; $('.descrrp').each(function(){
			   m++; var newID='descrrp'+m;
               $(this).attr('id',newID);       
			   });
			   
			   var n=0; $('.desccost').each(function(){
			   n++; var newID='desccost'+n;
               $(this).attr('id',newID);       
			   });
			   
			   var o=0; $('.webbing').each(function(){
			   o++; var newID='webbing'+o;
               $(this).attr('id',newID);       
			   });
			   
			   var p=0; $('.webrrp').each(function(){
			   p++; var newID='webrrp'+p;
               $(this).attr('id',newID);       
			   });
			   
			   var q=0; $('.webcost').each(function(){
			   q++; var newID='webcost'+q;
               $(this).attr('id',newID);       
			   });
			   
			   var r=0; $('.webbing-list').each(function(){
			   r++; var newID='webbing-list'+r;
               $(this).attr('id',newID);       
			   });
			   
			   var s=0; $('.qtylen').each(function(){
			   s++; var newID='qtylen'+s;
               $(this).attr('id',newID);       
			   });
			   
			   var t=0; $('.paint-list').each(function(){
			   t++; var newID='paint-list'+t;
               $(this).attr('id',newID);       
			   });
			   
			   var u=0; $('.paintdesc').each(function(){
			   u++; var newID='paintdesc'+u;
               $(this).attr('id',newID);       
			   });
			   
			   var v=0; $('.paintrrp').each(function(){
			   v++; var newID='paintrrp'+v;
               $(this).attr('id',newID);       
			   });
			   
			   var w=0; $('.paintcost').each(function(){
			   w++; var newID='paintcost'+w;
               $(this).attr('id',newID);       
			   });
			   
			   var x=0; $('.xlength').each(function(){
			   x++; var newID='xlength'+x;
               $(this).attr('id',newID);       
			   });
			   
			   var y=0; $('.uom').each(function(){
			   y++; var newID='uom'+y;
               $(this).attr('id',newID);       
			   });
			   